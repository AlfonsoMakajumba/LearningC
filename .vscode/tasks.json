{
    "version": "2.0.0",
    "tasks": [
        {
            "label" : "opierdol mi kolbe",
            "command" : "gcc",
            "type" : "shell",
            "args" : ["-g", "${file}", "-o", "${fileDirname}/${fileBasenameNoExtension}.exe"],
            
        }
    ]
}